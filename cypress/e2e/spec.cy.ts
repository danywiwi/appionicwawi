/* describe('My First Test', () => {
  it('Visits the initial project page', () => {
    cy.visit('/');
    cy.get('body').then((body) => {
      cy.log(body.text());
    });
    cy.get('selector-del-elemento', { timeout: 10000 }).should('contain.text', 'app is running'); 
  });
}); esta prueba esta mala
 */